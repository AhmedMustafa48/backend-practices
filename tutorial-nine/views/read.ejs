<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <a class="text-blue-500" href="/">Back To Home</a>
      <h1 class="text-3xl tracking-tighter mb-3 mt-10">All Users</h1>

      <div class="users flex gap-3 flex-wrap">
        <% if (users && users.length > 0) { %> <% users.forEach(function(user) {
        %>
        <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
          <div class="w-full h-52 rounded-lg bg-zinc-700 overflow-hidden">
            <img
              class="w-full h-full object-cover"
              src="<%= user.image %>"
              alt="<%= user.name %>"
            />
          </div>
          <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
          <h3 class="text-zinc-500"><%= user.email %></h3>
          <div class="flex gap-3 mt-3">
            <a href="/edit/<%= user.id %>" class="text-zinc-600">Edit User</a>
            <a href="/delete/<%= user.id %>" class="text-red-500"
              >Delete User</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <div class="text-zinc-400 mt-10 text-lg">
          No users found. Try adding some users first.
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
